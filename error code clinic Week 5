#Prediction = Zero division errorx = 10
x = 10
y = 0

if y != 0:
    result = 10 / y
    print("Result:", result)
else:
    print("Error: Cannot divide by zero.")

    # Prediction = Index error
    numbers = [1, 2, 3, 4, 5]
for i in range(len(numbers)):
 print(numbers[i])
 #Prediction = Syntax error
 def calculate_area(radius):
    area = 3.14 * radius ** 2
    return area
radius = 5
print(calculate_area(radius))
#Prediction = Syntax error
def is_even(number):
    if number % 2 == 0:
        return True
    else:
        return False

print(is_even(4))
print(is_even(7))
#Prediction = Syntax error
for i in range(5):
    print(i)
    # Prediction = Logic error
def greet(name):
    return f"Hello, {name}"

print(greet("Alice"))
#Prediction = Indentation error
numbers = [1, 2, 3, 4, 5]
total = 0

for number in numbers:
    total += number

print("Sum of numbers:", total)
# prediction = Indentaion error
def factorial(n):
    if n == 0:
        return 1
    else:
        return n * factorial(n - 1)

print(factorial(5))
#Prediction = Logic error
name = "Charlie"
if name == "Alice" or name == "Bob":
 print("Hello, friend!")
else:
 print("Hello, stranger!")
 # Prediction = Zero division error
 def divide_numbers(x, y):
    if y == 0:
        return "Error: Cannot divide by zero."
    result = x / y
    return result
num1 = 10
num2 = 0
print(divide_numbers(num1, num2))
